services:
  personalos:
    build: .
    container_name: personalos
    restart: unless-stopped
    ports:
      - "127.0.0.1:8090:8080"
    volumes:
      - ./data:/app/data
    environment:
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - GITHUB_USERNAME=${GITHUB_USERNAME}
      - SERVER_HOST=${SERVER_HOST}
      - SERVER_SSH_USER=${SERVER_SSH_USER:-root}
      - SERVER_SSH_PASS=${SERVER_SSH_PASS}
